language: java
git:
  depth: false
jdk:
  - openjdk11
cache:
  directories:
    - "$HOME/.m2"
before_install: 
  - chmod +x ./script/cibuild

script: ./script/cibuild

branches:
  only:
  - build

sudo: false

notifications:
  email: false